# マンセル発動時のアシスタントブロック色統一問題に関する原因調査・修正プラン

## 1. 原因の特定
現在、アシスタント自身のタイムライン上に描画されるヘルプ用の「仮想ブロック（Virtual Block）」は、`mainView.js` の描画ロジック内で、**予約のメニューカラー（ベースメニューの `colorCode`）をそのまま継承して描画**する仕組みになっています。
マンセル（チーム対応）としてアサインされた場合でも、システム上はその時間帯の特定スロット（シャンプーやカラーなど）にアサインされているというデータ構造であるため、結果として「マンセル中なのに単独で特定のメニュー（例：カラー）をずっと担当している」ように見えてしまい、現場が状況を正確に把握できない原因となっています。

## 2. 対策と改善プラン（修正内容）
アシスタントのヘルプ用仮想ブロックを描画する際（`mainView.js` 内）、そのアサインが「マンセルのチーム対応中」であるか否かを、計算エンジンから返却される `result.manncells` のデータと照合して判定します。
マンセル対応中であると判定された場合、以下のスタイルを適用します。

* **カラーの強制上書き:** メニューの色ではなく、マンセルの数字（チーム制バッジ）に合わせた**黄色（Yellow系）**を強制適用します。
* **くすみ・透明度の調整:** 他の仮想ブロック（メニューカラー）と馴染むように、透明度や黄色の濁り具合（例: `rgba(234, 179, 8, 0.15)` など）を調整し、悪目立ちしすぎない「連動している感」を演出します。
* **UIクラスの追加:** `.manncell-help-block` などの専用クラスを付与し、将来的なCSS調整も容易にします。

## 3. 仕様書との整合性と考慮点（仕様書のアップデート）
* **整合性:** 仕様書の「4-9. チーム対応UIにおける時間表示の原則」や「3-3. 業務の偏り可視化」の理念において、現場のスタッフが「いつからチーム対応に入ったか」を正確に把握することは極めて重要であるとされています。したがって、アシスタント行をマンセルカラー（黄色）で可視化することは、アプリの設計思想と完全に一致します。
* **仕様書の修正・追記が必要な点:** 仕様書の「3-2. レスキュー・ヘルプ時のUI描画と色分けの厳格化」のセクションでは、現在アシスタントのヘルプUIは「通常（通常色）」「隙間（黄色点線）」「召喚（赤色）」「特殊（金色）」の4つに厳格に切り分けられており、「マンセル時のアシスタントブロック」に関する明記が漏れています。今回の実装にあたり、**仕様書のこのセクションに「マンセル対応中のヘルプ表示ルール（黄色系を使用）」を追記する必要があります**。

## 4. ユーザーへの質問・確認事項
実装に進む前に、以下の点について方針をすり合わせたいと考えています。

1. **「隙間ヘルプ」との視覚的な区別について**
   現在、「隙間ヘルプ（頑張れ配置など）」の仮想ブロックにも黄色（点線枠＋☆バッジ等）が使用されています。今回追加する「マンセル対応（黄色）」と見分けがつくように、マンセルの場合は**「実線の黄色枠」にする**、または**ブロック内に「🤝」や「チーム」などの小さなバッジを付ける**といった工夫を追加してもよろしいでしょうか？
2. **色味（透過度）の調整について**
   ご要望の通り、透明度やくすみ加減は他のメニューブロックと同等の薄さ（例えば背景は15%程度の透過、枠線は同色）で調整します。もし実際の実装を見て「もっと薄く」などの微調整が必要であれば、実装後にお知らせください。

---
上記のプランと質問内容をご確認ください。Geminiなどにご相談いただき、ブラッシュアップやアドバイスを受けた上で、この方針で問題ない（または修正の要望がある）というOKが出ましたら、実際の修正・実装作業に入らせていただきます。
